#include<stdio.h>
int main()
{
	int n,rem;
	printf("enter the number:");
	scanf("%d",&n);
		printf("the digits of the given number are:");
	while(n!=0)
	{
		rem=n%10;
		printf("\n%d",rem);
		n=n/10;
	}
	return 0;
}
