#include<stdio.h>
int main ()
{
	int n,i,sum=0;
	printf("Enter the number:");
	scanf("%d",&n);
	for(i=1;i<=n;i++)
	{
		if(i%2==0)
		{
		sum=sum+(i*i);
	    }
	}
	printf("The sum of even numbers is: %d",sum);
	return 0;
}
