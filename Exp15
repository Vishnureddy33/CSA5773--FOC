#include<stdio.h>
int main()
{
	int n,temp,rem,sum=0;
	printf("enter the number:");
	scanf("%d",&n);
	temp=n;
	while(n!=0)
	{
		rem=n%10;
	    sum=sum+rem;
		n=n/10;
	}
	printf("the sum of digits of %d is %d",temp,sum);
	return 0;
}
